<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Facespace</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header class="header">
    <a href="/"><h1 class='head-title'>Facespace</h1></a>
    <% if (currentUser.name === undefined) { %>
      <a class="signin-button" href="/signin">Sign in</a>
    <% } else { %>
      <a class="signin-button" href=/users/<%=currentUser._id%>>Howdy, <%= currentUser.name %></a>
    <% } %>
  </header>